diff --git a/CMakeModules/GenerateSCMRev.cmake b/CMakeModules/GenerateSCMRev.cmake
index 1ae608c085..f45c556d77 100644
--- a/CMakeModules/GenerateSCMRev.cmake
+++ b/CMakeModules/GenerateSCMRev.cmake
@@ -8,19 +8,37 @@
 include(GetSCMRev)
 
 function(get_timestamp _var)
-    string(TIMESTAMP timestamp UTC)
+    string(TIMESTAMP timestamp "%Y-%m-%d" UTC)
     set(${_var} "${timestamp}" PARENT_SCOPE)
 endfunction()
 
 get_timestamp(BUILD_DATE)
 
+function(git_count _var)
+    execute_process(
+        COMMAND "${GIT_EXECUTABLE}" rev-list --count HEAD
+        WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}"
+        RESULT_VARIABLE res
+        OUTPUT_VARIABLE out
+        ERROR_QUIET
+        OUTPUT_STRIP_TRAILING_WHITESPACE
+    )
+
+    if(NOT res EQUAL 0)
+        set(out "${out}-${res}-NOTFOUND")
+    endif()
+
+    set(${_var} "${out}" PARENT_SCOPE)
+endfunction()
+
+git_count(GIT_COUNT)
+
 if (DEFINED GIT_RELEASE)
   set(BUILD_VERSION "${GIT_TAG}")
   set(GIT_REFSPEC "${GIT_RELEASE}")
   set(IS_DEV_BUILD false)
 else()
-  string(SUBSTRING ${GIT_COMMIT} 0 10 BUILD_VERSION)
-  set(BUILD_VERSION "${BUILD_VERSION}-${GIT_REFSPEC}")
+  set(BUILD_VERSION "${BUILD_DATE}-${GIT_COUNT}")
   set(IS_DEV_BUILD true)
 endif()
 
@@ -28,7 +46,7 @@ set(GIT_DESC ${BUILD_VERSION})
 
 # Generate cpp with Git revision from template
 # Also if this is a CI build, add the build name (ie: Nightly, Canary) to the scm_rev file as well
-set(REPO_NAME "Eden")
+set(REPO_NAME "Eden Nightly")
 set(BUILD_ID ${GIT_REFSPEC})
 set(BUILD_FULLNAME "${REPO_NAME} ${BUILD_VERSION} ")
 set(CXX_COMPILER "${CMAKE_CXX_COMPILER_ID} ${CMAKE_CXX_COMPILER_VERSION}")
@@ -36,6 +54,6 @@ set(CXX_COMPILER "${CMAKE_CXX_COMPILER_ID} ${CMAKE_CXX_COMPILER_VERSION}")
 # Auto-updater metadata! Must somewhat mirror GitHub API endpoint
 set(BUILD_AUTO_UPDATE_WEBSITE "https://github.com")
 set(BUILD_AUTO_UPDATE_API "http://api.github.com")
-set(BUILD_AUTO_UPDATE_REPO "eden-emulator/Releases")
+set(BUILD_AUTO_UPDATE_REPO "pflyly/eden-nightly")
 
 configure_file(scm_rev.cpp.in scm_rev.cpp @ONLY)
diff --git a/src/android/app/build.gradle.kts b/src/android/app/build.gradle.kts
index 01a413261e..0f6fc0f114 100644
--- a/src/android/app/build.gradle.kts
+++ b/src/android/app/build.gradle.kts
@@ -306,18 +306,13 @@ fun runGitCommand(command: List<String>): String {
 }
 
 fun getGitVersion(): String {
-    val gitVersion = runGitCommand(
+    val commitCountString = runGitCommand(
         listOf(
             "git",
-            "describe",
-            "--always",
-            "--long"
+            "rev-list",
+            "--count",
+            "HEAD"
         )
-    ).replace(Regex("(-0)?-[^-]+$"), "")
-    val versionName = if (System.getenv("GITHUB_ACTIONS") != null) {
-        System.getenv("GIT_TAG_NAME") ?: gitVersion
-    } else {
-        gitVersion
-    }
-    return versionName.ifEmpty { "0.0" }
+    ).trim()
+    return (commitCountString.toIntOrNull() ?: 1).toString()
 }
diff --git a/src/android/app/src/main/java/org/yuzu/yuzu_emu/fragments/AboutFragment.kt b/src/android/app/src/main/java/org/yuzu/yuzu_emu/fragments/AboutFragment.kt
index edd1b0765d..da22065ce8 100644
--- a/src/android/app/src/main/java/org/yuzu/yuzu_emu/fragments/AboutFragment.kt
+++ b/src/android/app/src/main/java/org/yuzu/yuzu_emu/fragments/AboutFragment.kt
@@ -85,6 +85,10 @@ class AboutFragment : Fragment() {
 
         binding.textVersionName.text = fullVersionText
         binding.buttonVersionName.setOnClickListener {
+            openLink(
+                getString(R.string.nightly_link)
+            )
+
             val clipBoard =
                 requireContext().getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager
             val clip = ClipData.newPlainText(getString(R.string.build), fullVersionText)
diff --git a/src/android/app/src/main/res/values/strings.xml b/src/android/app/src/main/res/values/strings.xml
index 233e5afd43..d34555c427 100644
--- a/src/android/app/src/main/res/values/strings.xml
+++ b/src/android/app/src/main/res/values/strings.xml
@@ -448,6 +448,7 @@
     <string name="x_link" translatable="false">https://x.com/edenemuofficial</string>
     <string name="website_link" translatable="false">https://eden-emu.dev</string>
     <string name="github_link" translatable="false">https://git.eden-emu.dev/eden-emu</string>
+    <string name="nightly_link" translatable="false">https://github.com/pflyly/eden-nightly/releases/latest</string>
 
     <!-- General settings strings -->
     <string name="frame_limit_enable">Limit speed</string>
diff --git a/src/yuzu/about_dialog.cpp b/src/yuzu/about_dialog.cpp
index 9f7597f471..b73cd41c1e 100644
--- a/src/yuzu/about_dialog.cpp
+++ b/src/yuzu/about_dialog.cpp
@@ -34,7 +34,7 @@ AboutDialog::AboutDialog(QWidget* parent)
     }
     ui->labelBuildInfo->setText(
         ui->labelBuildInfo->text().arg(QString::fromStdString(yuzu_build_version),
-                                       QString::fromUtf8(Common::g_build_date).left(10)));
+                                       QString::fromUtf8(Common::g_build_id)));
 }
 
 AboutDialog::~AboutDialog() = default;
